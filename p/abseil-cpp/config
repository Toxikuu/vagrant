upstream = "abseil/abseil-cpp"

# TODO: not entirely sure why vsort sorts rcs wrong here
unstable.fetch = "gr | sort -V"

unstable.expected = '''^[0-9]{8}\.(rc[0-9]+|[0-9]+)$'''
release.expected = '''^[0-9]{8}\.[0-9]+$'''
