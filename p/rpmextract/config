upstream = "https://gitlab.archlinux.org/archlinux/packaging/packages/rpmextract.git"

[[channels]]
name = "release"
fetch = "gr | vtrim -e 's,-.*,,' | vsort"

[[channels]]
name = "commit"
fetch = "githead" # archlinux.org is flagged as the arch upstream type, so the fetch must be manually specified
