upstream = "gnome/glib-networking"

[[channels]]
name = "release"
fetch = '''gr | vtrim -e 's,^glib-,,' | vfs | grep -v '\.9[0-9]$' | vsort'''

[[channels]]
name = "commit"

# TODO: Add unstable once I make vsort properly sort prerelease versions with
# fewer parts
